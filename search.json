---
---
{
	{% for post in site.posts %}"{{ forloop.rindex }}": {
		"title": "{{ post.title }}",
		"url": "{{ site.url }}{{ post.url }}",
		"category": "{{ post.categories }}"	
	}{% if forloop.last == false %},{% endif %}{% endfor %}
}
